[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && npm install --omit=dev && npm run build && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
